name: CodeEdit Issues
on:
  issues:
    types:
      - opened

jobs:
  codeEditIssues:
    runs-on: ubuntu-latest
    steps:
      - uses: nanashili/GitHubBot@Initial
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
